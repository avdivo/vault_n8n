# Список доработок (TODO) и известные проблемы

Здесь документируются предложения по улучшению, новые функции, а также известные ошибки и проблемы, которые предстоит решить в VaultN8N.

## Запланированные доработки / Улучшения:

1.  **Документация:**
    *   Уточнить описание настроек нод в документации (`README.md`), особенно для интеграции с n8n.
2.  **Валидация ключей:**
    *   Запретить создание ключей, содержащих специальные символы, такие как `*` (для предотвращения нежелательного поведения при поиске по шаблонам).
3.  **Обработка дубликатов при выводе:**
    *   Реализовать логику для исключения дубликатов при запросе нескольких ключей, которые могут возвращать одинаковые секреты (например, при использовании шаблонов).
4.  **Примеры интеграции:**
    *   Подготовить файл с JSON-конфигурацией рабочего процесса n8n с примерами использования нод для VaultN8N.

## Известные ошибки:

- Пока не обнаружено критических ошибок.